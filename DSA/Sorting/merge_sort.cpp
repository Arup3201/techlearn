#include <bits/stdc++.h>

void mergeSort(int a[], int l, int h)
{
}

int main()
{
    int a[] = {20, 1, 30, 5, 8, 40, 31};
    int n = sizeof(a) / sizeof(a[0]);
    int l = 0, h = n - 1;
    mergeSort(a, l, h);
    return 0;
}